.claude/settings.local.json

__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.idea/
.vscode/
*.log

# Local artifacts (per-run report at the repo root is regenerated each run)
listings.db
listings.db-journal
report.html
diff.html
/styles.css
.cache/
playwright-debug/

# out/ is tracked so the daily commit can record CSV + diff history.
# Keep the daily run log + auto-copied styles.css out of git.
out/daily.log
out/styles.css

# docs/ is the GitHub Pages root — tracked so Pages can serve it.
# Photos under docs/photos/ ARE tracked (Pages needs them); main.py
# prunes unreferenced files each run so the cache doesn't bloat.
